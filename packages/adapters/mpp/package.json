{
  "name": "@suverse-pay/adapter-mpp",
  "version": "0.0.0",
  "private": true,
  "description": "Adapter for the Machine Payments Protocol (MPP) — 402-protocol payments. v1 ships the tempo method on Tempo Moderato testnet via direct JSON-RPC; stripe method + session/subscription intents arrive in v2 when Stripe publishes the REST surface.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@suverse-pay/core-types": "workspace:*",
    "@suverse-pay/provider-sdk": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  }
}
